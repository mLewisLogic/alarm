platform :ios, '10.3'
use_frameworks!

target 'alarm' do
  inherit! :search_paths
  pod 'EDSunriseSet', '~> 1.0'
  pod 'MagicalRecord', '~> 2.2'
end

target 'alarmTests' do
  inherit! :search_paths
  # pod 'Quick'
  # pod 'Nimble'
end
